import React from 'react';
import { Button } from '@mui/material';

const Favorite = ({ key, id, title, company }) => (
  <Container>
    <Typography variant="h3">
      Favorite
    </Typography>
  </Container>
);

export default Favorite;