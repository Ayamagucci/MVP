require('dotenv').config();
const { PORT, API_URL, API_ID, API_KEY } = process.env;

// COME BACK TO THIS
module.exports = {
  getAll: async(req, res) => {},

  getFaves: async(req, res) => {},

  saveJob: async(req, res) => {}
};